// Vamos falar sobre ponteiros em C++. 
// Ponteiro vazio ou NULL 
// É um ponteiro que não aponta para nenhum endereço de memória válido. Ele é usado para indicar que o ponteiro não está associado a nenhum objeto ou variável. 
// Em C++, podemos inicializar um ponteiro vazio usando o valor NULL ou nullptr (em C++11 e versões posteriores).
#include <cstddef>

using namespace std;
 int main() {
 bool*truth = NULL; // Ponteiro vazio usando NULL
 float*money = NULL; // Ponteiro vazio usando NULL
 // Estou usando NULL por conta do livro


    return 0;
}